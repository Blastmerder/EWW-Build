(include "objects/battary.yuck")
(include "objects/clock.yuck")
(include "objects/language.yuck")
(include "objects/volume.yuck")
(include "objects/wifi.yuck")

(defwidget right []
    (box :orientation "h" :space-evenly false :halign "end" :class "info_bar"
;; ToDo WiFi module, System module (ram)
        (wifi)
            
        (label 
            :text "|"
            :class "spliter"
        )   

        (battary)
        
        (label 
            :text "|"
            :class "spliter"
        )   
        
        (volume)
        
        (label 
            :text "|"
            :class "spliter"
        )    

        (language)
        
        (label 
            :text "|"
            :class "spliter"
        )    

        (clock)
    )
)